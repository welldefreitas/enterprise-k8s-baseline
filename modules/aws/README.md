# AWS Modules (TODO)

This folder is a placeholder for EKS baseline modules to match the interface in `stacks/gcp-gke`.
Planned modules:
- network (VPC/subnets/NAT/SG)
- iam (least-privilege roles + IRSA)
- eks (private cluster + nodegroups)
