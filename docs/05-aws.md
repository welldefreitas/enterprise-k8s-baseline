# AWS (EKS) - TODO

This stack is scaffolded to preserve the cloud-agnostic interface.
Planned controls:
- Private EKS (private endpoint by default)
- VPC + subnets + NAT
- IRSA (OIDC federation)
- Least-privilege IAM roles
